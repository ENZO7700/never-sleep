# Optional: enables client-side Gemini features when set.
# Copy to .env.local for local development. Never commit real values.
VITE_GEMINI_API_KEY=

# Optional: webhook URL for /api/contact form submissions (server-side only).
# When unset, the contact form returns HTTP 503 with { "error": "not configured" }.
CONTACT_WEBHOOK_URL=
